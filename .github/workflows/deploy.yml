name: CD - Deploy via Coolify

on:
  push:
    branches: [ main ]

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Trigger Coolify deployment
        run: |
          curl -s -X GET "${{ secrets.COOLIFY_WEBHOOK }}" \
            -H "Accept: application/json"
